import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udemycourse.ionang',
  appName: 'Ion-Ang-course',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
